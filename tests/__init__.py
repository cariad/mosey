"""Tests for the `mosey` package.

You must develop and run these tests in a supported configuration (see
https://cariad.github.io/mosey/unsupported/) with a UTF-8 file system encoding. That's
every Windows and macOS machine, every CI runner, and nearly every Linux machine.

Mosey itself works with other file system encodings on Linux, but its tests assume UTF-8
and won't run without it.
"""
