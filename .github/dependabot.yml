version: 2

updates:
  # Bump the SHA-pinned actions in `.github/workflows/`.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly

  # Bump the locked dependency versions in `uv.lock`.
  - package-ecosystem: uv
    directory: /
    schedule:
      interval: weekly
